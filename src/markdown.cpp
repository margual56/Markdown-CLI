#include <stdio.h>      // Standard I/O
#include <string>       // Use strings
#include <fstream>      // Create, write and read files

#include "HTML.hpp"

 //Read from a file, return a HTML object
HTML markdownFile(std::fstream f, char **flags){

    return HTML();
}

//Read markdown from stdin, return HTML object
HTML markdownStdin(int size, char *buffer, char **flags){

    return HTML();
}

